#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Enforce conventional commits format:
# type(scope): description
# e.g. feat(map): add timeline slider, fix(auth): handle expired JWT
npx --no -- commitlint --edit "$1"
